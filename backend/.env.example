## Example environment variables for the backend service

# Port for the Express server
PORT=4000

# MongoDB connection string. In production this should be a remote database.
MONGODB_URI=mongodb://localhost:27017/emergency_db

# Secret key used for signing JWT tokens
JWT_SECRET=your_jwt_secret_here

# Base URL for the FHIR server
FHIR_BASE_URL=https://fhirserver.example.com

# SMTP configuration for sending emails (used by utils/emailService.js)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password
